import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    alignItems: 'center',
    marginTop: 10,
    padding: 10,
    width: 200,
    backgroundColor: 'white',
  },
  buttonText: {
    fontSize: 12,
    color: 'black',
  },
});
